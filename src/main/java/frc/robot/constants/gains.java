package frc.robot.constants;

public interface gains {
    public interface turnToDegreePID {
        double kp = 0.0; //TODO: tune values
        double kd = 0.0;
        double kki = 0.0;
        
    }
}
